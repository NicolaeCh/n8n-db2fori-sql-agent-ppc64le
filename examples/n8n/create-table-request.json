{
  "sql": "CREATE TABLE MONAI.ALERT_AUDIT (ALERT_ID VARCHAR(128) NOT NULL, STATUS VARCHAR(32) NOT NULL, CREATED_AT TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, UPDATED_AT TIMESTAMP, PRIMARY KEY (ALERT_ID))",
  "parameters": []
}
